import g4p_controls.*;

void setup(){
  size(800, 500);
  background(0);
  createGUI();
  strategyWindow.setVisible(false);
  addStrategy.setVisible(false);
  removeStrategyWindow.setVisible(false);
  
}

void draw(){

}
